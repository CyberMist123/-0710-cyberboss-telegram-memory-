const test=require("node:test"),assert=require("node:assert/strict"),fs=require("node:fs"),os=require("node:os"),path=require("node:path");
const {MemoryNoteService}=require("../src/services/memory-note-service");
test("memory.note persists a bounded private daily budget",()=>{const root=fs.mkdtempSync(path.join(os.tmpdir(),"cb-note-")),now=()=>new Date("2026-07-20T10:00:00.000Z"),service=new MemoryNoteService({continuityDir:root,now});for(let i=0;i<10;i+=1)assert.equal(service.note({text:`note ${i}`,quote:i===0?"private quote":""}).ok,true);assert.equal(service.note({text:"eleven"}).error,"budget_exhausted");assert.match(fs.readFileSync(path.join(root,"ai_self_notes.md"),"utf8"),/\[收藏\]/u);assert.doesNotMatch(fs.readFileSync(path.join(root,".jobs","memory-note-audit.jsonl"),"utf8"),/private quote|note 0/u);});

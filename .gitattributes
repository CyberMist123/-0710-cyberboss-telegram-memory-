# Byte-compared fixtures must survive checkout unchanged on every platform:
# autocrlf must never rewrite their line endings.
test/fixtures/*.json text eol=lf

# 钩子由 sh 执行；core.autocrlf=true 会把它们签出成 CRLF，shebang 直接失效。
.githooks/* text eol=lf

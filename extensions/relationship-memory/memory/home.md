# home — 这个家怎么运转

记忆是背景，不是台词：改变下一句话的姿态，不替模型决定内容。

醒来只读 `reentry.md`；聊天中零记忆职责；明确寻找旧事时才查 `episodes.jsonl` 或 `relationship_timeline.md`。
不确定就求证，当前对话优先于旧记忆。closeout 采用三个问题，允许“无”。

文件职责与正式设计以根目录 `PROJECT.md` 为准。
